# Uncomment the next line to define a global platform for your project
platform :ios, '8.0'

target 'RamenPos' do
    # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
    use_frameworks!
    
    # Pods for RamenPos
    #pod 'SPIClient-iOS', '~> 2.4.4'
    pod 'SPIClient-iOS', :git => 'https://github.com/AssemblyPayments/spi-client-ios', :branch => 'feature/v2.4.x'
    #pod 'SPIClient-iOS', :path => '~/Documents/GitHub/spi-client-ios/feature-v2.4.x'
    
    target 'RamenPosTests' do
        inherit! :search_paths
        # Pods for testing
    end
    
    target 'RamenPosUITests' do
        inherit! :search_paths
        # Pods for testing
    end
    
end
